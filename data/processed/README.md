# Dataset procesado

Contendrá `train/`, `validation/` y `test/` después de la auditoría. La separación debe realizarse antes del aumento de datos. Validación y prueba conservan únicamente imágenes reales.
