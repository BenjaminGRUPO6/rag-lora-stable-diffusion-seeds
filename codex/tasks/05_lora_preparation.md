# Tarea 05: preparar LoRA

Crea un dataset de imágenes y `metadata.jsonl` para SD 1.5 + LoRA. Usa captions coherentes con la clase y trigger word `soybeanseed`. No entrenes todavía. Valida archivos y crea pruebas.
